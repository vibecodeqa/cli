/** Re-exports from src/diagrams/ — keeps existing import paths working. */
export { generateArchSVG, generateDSM, generateContainerDiagram, generateLayerDiagram, generatePackageDiagram, generateSequenceDiagram } from "../diagrams/index.js";
